# Copy to .env only when the storefront URL needs a local override.
# Runner settings are not environment configuration: their defaults live in
# config.py and are overridden per run, for example
#   robot --variable HEADLESS:false tests

BEE_TICK_BASE_URL=https://btech.com/en
